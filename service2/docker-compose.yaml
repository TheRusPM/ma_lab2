version: "3.8"

services:
  service2:
    build: .
    container_name: svc_service2
    environment:
      - SERVICE_NAME=Service-2
      - PORT=3000
    expose:
      - "3000"
    networks:
      - lab2-net
    restart: unless-stopped

networks:
  lab2-net:
    external: true
